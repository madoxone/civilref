// Convenience re-export so consumers don't need to know about the context object.
// Usage:  const { projects, current, actions } = useProjects();
export { useProjects } from '../contexts/ProjectContext.jsx';
